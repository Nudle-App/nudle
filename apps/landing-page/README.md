# @nudle/landing-page

Static marketing site (HTML/CSS/JS). No build step required.

```bash
pnpm --filter @nudle/landing-page dev      # http://localhost:5175
pnpm --filter @nudle/landing-page build    # copies files into dist/
```

Deploy the `dist/` folder as a static site.
