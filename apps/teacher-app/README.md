# @nudle/teacher-app

Vite + React teacher dashboard. Auth via better-auth against `@nudle/server`.

```bash
# apps/teacher-app/.env
VITE_API_URL=http://localhost:3001

pnpm --filter @nudle/teacher-app dev
```

Port `5174`.
