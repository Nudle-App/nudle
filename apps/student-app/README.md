# @nudle/student-app

Vite + React student app. Auth via better-auth against `@nudle/server`.

```bash
# apps/student-app/.env
VITE_API_URL=http://localhost:3001

pnpm --filter @nudle/student-app dev
```

Port `5173`.
