# Student / teacher apps — copy into apps/<app>/.env
# Local: point at the Express API (cookies + CORS).
# Production: your deployed API URL (set at build time on Render).
VITE_API_URL=http://localhost:3001

# Server: apps/server/.env — see apps/server/.env.example
# Landing page is static and needs no env.
